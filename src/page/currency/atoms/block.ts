import {styled} from "@mui/material";

export const CurrencyBlock = styled('div')({
    display: 'flex',
    marginTop: '20px',
    flexWrap: 'wrap'
})

export const CurrencyListBox = styled('div')({
    display: 'inline-flex',
    flexWrap: 'wrap',
    flexDirection: 'row',
    flex: '1',
})

export const ItemBox = styled('div')({
    marginBottom: '10px', width: '200px'
})